객체지향 프로그램은 대부분 클래스를 기반으로 둔다
자바스크립트는 프로토타입이다 (생성자 함수를 사용했었다 )
하지만 최신 자바스크립트에서는 클래스 기능이 추가되었다 (모던한 자바스크립)
이제는 생성자 함수를 쓰지 않는다... 

여기서 클래스란 
객체를 생성할 수 있는 템플릿(청사진, 틀)
객체지향 프로그래밍 (Object - oriented Programming)

클래스는 객체를 만들 수 있다 -> 이렇게 클래스를 통해 생성된 객체 = 인스턴스
라고 부른다. 

